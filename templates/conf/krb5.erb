#
# puppet managed file
#

[libdefaults]
	default_realm = <%= @realm %>
	<%- if defined?(@default_keytab) -%>
	default_keytab_name = FILE:<%= @catalina_base %>/conf/<%= @default_keytab %>
	<%- end -%>

	default_tkt_enctypes = arcfour-hmac-md5,rc4-hmac
	default_tgs_enctypes = arcfour-hmac-md5,rc4-hmac
	permitted_enctypes   = arcfour-hmac-md5,rc4-hmac
	forwardable=<%= @forwardable %>

	allow_weak_crypto=<%= @allow_weak_crypto %>

[realms]
	LIFECAPNET.COM  = {
    <%- @kdc.each do | eachkdc | -%>
		kdc = <%= eachkdc %>
    <%- end -%>
		default_domain = <%= @realm %>
}

[domain_realm]
	.<%= @realm %> = <%= @realm %>
	<%= @realm %> = <%= @realm %>
	.<%= @realm.downcase %> = <%= @realm %>
	<%= @realm.downcase %> = <%= @realm %>
